-> go inside folder from the root run following commands

to install client dependencies

1. `npm run client-install`

to install server dependencies

2. `npm install`

to run development server

3. `npm run dev`
